// Runtime configuration. Set apiBaseUrl to the Railway service URL in production.
// Leave it empty for local browser development (the Angular proxy handles /api).
window.__LATEXF_CONFIG__ = {
  apiBaseUrl: 'https://latexb.onrender.com'
};
